{
  "name": "@react-discovery/tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "cypress:open": "CYPRESS_baseUrl=http://localhost:3000 cypress open",
    "test": "CYPRESS_baseUrl=http://localhost:3000 cypress run"
  },
  "devDependencies": {
    "cypress": "^3.3.1"
  }
}
